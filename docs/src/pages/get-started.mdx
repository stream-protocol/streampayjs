---
title: 'Installation StreamPayJS'
description: 'Get starting building with StreamPayJS!'
---

import { Tabs, Tab } from "nextra-theme-docs"

# Getting Started 

StreamPayJS is a react component library for interacting with the Solana blockchain.

## Installation

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tab>
  ```
  npm install streampayjs
  ```
  </Tab>
  <Tab>
  ```
  yarn add streampayjs
  ```
  </Tab>
  <Tab>
  ```
  pnpm i streampayjs
  ```
  </Tab>
</Tabs>